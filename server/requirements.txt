# CPU-only PyTorch (keeps image small — no CUDA drivers needed)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
transformers==4.41.2
pandas==2.2.2
python-dotenv==1.0.1

# Tokenizer backends
sentencepiece==0.2.0
protobuf==4.25.3

# FastAPI extras
python-multipart==0.0.9
pydantic==2.7.4

# Instagram Graph API integration
httpx==0.27.0          # async HTTP client for Graph API calls
itsdangerous==2.2.0    # signs the server-side session cookie
