# Deployment requirements — NO PyTorch or Transformers
# Uses HuggingFace Serverless Inference API instead.
# RAM usage: ~80-100 MB → fits Render/Koyeb/Railway free tier (512 MB RAM)

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pandas>=2.2.2
python-dotenv>=1.0.1

# FastAPI extras
python-multipart>=0.0.9
pydantic>=2.10.0

# HTTP client (for HF Inference API calls + YouTube Data API)
httpx>=0.27.0

# YouTube OAuth 2.0 — comment deletion as channel owner
google-auth-oauthlib>=1.2.0
google-auth>=2.29.0
itsdangerous>=2.2.0
starlette>=0.37.0
